import { getConfig } from "../../config";

class eventService {
  //Signin
  static async create(email, password, options: any = {}) {
    try {
      return true;
    } catch (error) {
      throw error;
    }
  }
}

export default eventService;
